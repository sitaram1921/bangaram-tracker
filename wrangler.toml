name = "bangaram-notifications"
main = "cloudflare-worker.js"
compatibility_date = "2024-12-01"

[vars]
FIREBASE_URL = "https://tracker-bea1e-default-rtdb.firebaseio.com"

# Secrets (never put these in this file — set via Cloudflare Dashboard or CLI):
# Run: wrangler secret put CLAUDE_API_KEY
# Run: wrangler secret put VAPID_PRIVATE_KEY

[triggers]
crons = ["*/15 * * * *"]
